import {
    handleUp,
    handleCd,
    handleLs
} from './navigation-and-working-directory.js'

import {
    handleCat,
    handleAdd,
    handleRn,
    handleCp,
    handleMv,
    handleRm,
} from './basic-operations.js'

import { handleOs } from './operating-systems.js'

import { handleHash } from './hash.js'

import { handleCompress, handleDecompress } from './compression.js'

export {
    handleUp,
    handleCd,
    handleLs,
    handleCat,
    handleAdd,
    handleRn,
    handleCp,
    handleMv,
    handleRm,
    handleOs,
    handleHash,
    handleCompress,
    handleDecompress
}
